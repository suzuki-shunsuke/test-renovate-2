{
  "extends": [
    "config:base"
  ],
  "regexManagers": [
    {
      "fileMatch": ["\\.?aqua\\.ya?ml"],
      "matchStrings": [
        "^ +(version|ref): (?<currentValue>[^'\"\\s]*) +# renovate: depName=(?<depName>.*)",
        "^ +(version|ref): \"(?<currentValue>[^\\s]*)\" +# renovate: depName=(?<depName>.*)",
        "^ +(version|ref): '(?<currentValue>[^\\s]*)' +# renovate: depName=(?<depName>.*)",
        "name: (?<depName>[^'\"]*)@(?<currentValue>[^\\s]*)",
        "name: \"(?<depName>[^']*)@(?<currentValue>[^\\s]*)\"",
        "name: '(?<depName>[^\"]*)@(?<currentValue>[^\\s]*)'"
      ],
      "datasourceTemplate": "github-releases"
    }
  ]
}
